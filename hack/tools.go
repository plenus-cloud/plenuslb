package tools

import (
	// required for code generation, must be in vendor folder
	_ "k8s.io/code-generator"
	// required for code generation, must be in vendor folder
	// _ "k8s.io/code-generator/hack"
)
